#!/bin/bash

pushd linux > /dev/null
mkdir -p build
cp ../linux_config build/.config
make -s -k -j $(getconf _NPROCESSORS_ONLN) O=build
mkdir -p ../images/linux/
cp build/arch/x86/boot/bzImage ../images/linux/
popd > /dev/null
